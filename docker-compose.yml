# SK Store — development Compose (optional / placeholder)
#
# Local development typically uses PostgreSQL on the host — see README.md.
# Production Docker stack: docker-compose.prod.yml

services: {}
