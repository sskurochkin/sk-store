# Server

NestJS API for SK Store with Prisma.

```bash
npm install
npx prisma generate
npm run start:dev
```

Default port: `3001` (`PORT` in `.env`).

Quality checks: `npm run typecheck`, `npm run lint`, `npm run build`.
